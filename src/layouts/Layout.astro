---
import "../styles/global.css";
import "../styles/prism.css";
import Navigation from "../components/Navigation.astro";

interface Props {
	title: string;
	showHeader?: boolean;
}

const { title, showHeader = true } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="AI-Created Portfolio Website" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="min-h-screen bg-white bg-grid-pattern bg-grid">
		{
			showHeader && (
				<div class="container mx-auto px-4 py-16 max-w-4xl">
					<div class="text-center space-y-4">
						<h1 class="text-7xl font-bold animated-gradient-text tracking-tight">
							AI + Tim Cane
						</h1>
						<p class="text-2xl font-semibold text-gray-600">
							100% AI-Generated Portfolio
						</p>
					</div>
				</div>
			)
		}

		<Navigation />
		<slot />

		<script>
			import Prism from "prismjs";
			// Import additional languages
			import "prismjs/components/prism-typescript";
			import "prismjs/components/prism-javascript";
			import "prismjs/components/prism-jsx";
			import "prismjs/components/prism-tsx";
			import "prismjs/components/prism-bash";
			import "prismjs/components/prism-markdown";
			import "prismjs/components/prism-json";
			import "prismjs/components/prism-css";

			// Highlight all code blocks
			Prism.highlightAll();
		</script>
	</body>
</html>
